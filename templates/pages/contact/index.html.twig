{% extends 'base.html.twig' %}

{% block title %}Jte dis quoi - Contact{% endblock %}

{% block body %}
<div class="container mt-5">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-5">
            {{ message }}
        </div>
        {% endfor %}
    <h1>Formulaire de contact</h1>
    {{  form (form)  }}
</div>
{% endblock %}
